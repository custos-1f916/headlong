[Unit]
Requires=custos-boundary.service
After=custos-boundary.service

[Service]
# Reapply atomically on every CT122 start, including manual pct starts.
# A policy load failure prevents this container from starting, not other guests.
ExecStartPre=/usr/sbin/nft -f /etc/custos-boundary.nft
